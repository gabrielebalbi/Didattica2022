#include <xuartlite_l.h>
#include <xintc_l.h>
#include <xparameters.h>
#include <stdio.h>
#include <string.h>
#include "xgpio.h"
#include "xtrafgen.h"

#include "xsysmon.h"
#include "xil_cache.h"
#include "sleep.h"

#define XADC_DEVICE_ID (XPAR_XADC_WIZ_0_DEVICE_ID)

// TODO: create an "xadc/" subdirectory of project src. make as portable to other boards/xadc configs as possible
typedef struct XadcInfo {
	int NumChannels;
	int Channels[7];
	int ChannelIsDiff[7];
	char *ChannelNames[7];
} XadcInfo;

typedef struct Xadc_ChannelInfo {
	int SequencerIndex;
	int IsDiff;
	char *Name;
} Xadc_ChannelInfo;



#define TEST_BUFFER_SIZE        50


#define GPIO_LED_ID XPAR_LED_BUT_GPIO_DEVICE_ID
#define LED_CHANNEL 1



#define GPIO_CTRL_ID XPAR_AXI_GPIO_0_DEVICE_ID
#define CTRL_CHANNEL 1

#define STOP_TF 29
#define START_TF 28
#define ENABLE_COUNTERS 26
#define INIT_COUNTERS 27
#define START_COUNTERS 25

#define TRAFGEN_BASE XPAR_AXI_TRAFFIC_GEN_0_BASEADDR
#define STREAMING_CTRL 0x30
#define STREAMING_CFG 0x34
#define STREAMING_TRANSFER_LENGTH 0x38
#define STREAMING_TRANSFER_COUNT 0x3C
#define STREAMING_ERROR_STATUS 0x70
#define STREAMING_ERROR_ENABLE 0x74
#define STATIC_MODE 0x60



#define XADC_STATUS_REG 0x4
#define XADC_STATUS_ALARM 0x8

#define XADC_CFG1 0x40
#define XADC_CFG2 0x41
#define XADC_CFG3 0x42

#define XADC_TEMP 0x200
#define XADC_VCCINT 0x204
#define XADC_VCCAUX 0x208
#define XADC_VPVN 0x20C

#define XADC_VREFP 0x210
#define XADC_VREFP 0x214

#define XADC_VCCBRAM 0x218
#define XADC_ADC_OFFSET 0x224
#define XADC_GAIN_ERROR 0x228

#define DELAY_COUNT 100000000 // Adjust this value as needed for your environment
#define BUFFER_SIZE 1000 // Define a buffer size for storing samples


XGpio Gpio;
XGpio GpioCtrl;
XTrafGen XTrafGenInstance;

volatile struct comando myComando;
unsigned int jj=0;
unsigned int kk=0;


volatile u8 ReceiveBuffer[TEST_BUFFER_SIZE];


struct comando {
	u8 commandReceived[2];
	u8 firstOp[10];
	u8 secondOp[10];
	char cr[2];
	int select; //0 write e 1 è read, -1 comando sconosciuto
};





void uart_int_handler(void *baseaddr_p);
void printMenu();

void Traffic_Gen_status();
int XTrafGenStremingModeMasterExample(XTrafGen *InstancePtr, u16 DeviceId);

void XADC_status();
void Xadc_Init(XSysMon *InstancePtr, const Xadc_ChannelInfo DeviceInfo[], const int NumChannels, const u32 DeviceId);
void Xadc_ReadData(XSysMon *InstancePtr, const Xadc_ChannelInfo DeviceInfo[], const int NumChannels, u16 RawData[]);



void printMenu()
{
	xil_printf ("####################################################### \r\n");
	xil_printf ("# AXI inspector: \r\n");
	xil_printf ("# Indirizzi e dati sono a 32bit formato HEX: \r\n");
	xil_printf ("# Leggi --->  rd indirizzo \r\n");
	xil_printf ("# Scrivi--->  wr indirizzo dato\r\n");
	xil_printf ("# INFO  --->  tg TRAFFIC GENERATOR INFO\r\n");
	xil_printf ("# INFO  --->  xa XADC INFO\r\n");
	xil_printf ("####################################################### \r\n");
}


volatile struct comando parser(u8* buffer);


/* uartlite interrupt service routine */
void uart_int_handler(void *baseaddr_p) {
	char c;
	c = XUartLite_RecvByte(XPAR_AXI_UARTLITE_0_BASEADDR);

	xil_printf ("%c",c);

	jj++;
	ReceiveBuffer[jj]=c;
	if ((c==0xD)) {
		xil_printf ("\r\n",c);
		xil_printf ("command received!\r\n\r\n");
		kk=jj;
		jj=0;

		for(int i=0;i<kk;i++){
				xil_printf("%c",ReceiveBuffer[i]);
		        }

		xil_printf ("\r\n",c);

		myComando=parser(&ReceiveBuffer);


		int address = (int)strtol(myComando.firstOp, NULL, 16);



		if (myComando.select==1)
			{
				xil_printf ("READ!\r\n");
				xil_printf("Address 0x%x---- >> 0x%x\n\r", address,Xil_In32((u32)address	));

			}
		else if (myComando.select==0)
					{
					int value = (int)strtol(myComando.secondOp, NULL, 16);
					xil_printf ("WRITE!\r\n");
					xil_printf("Pre  0x%x---- >> 0x%x\n\r", address,Xil_In32((u32)address	));
					Xil_Out32((u32)address, (u32)value);
					xil_printf("Post 0x%x---- >> 0x%x\n\r", address,Xil_In32((u32)address	));

					}
		else if (myComando.select==2)
					{
					xil_printf ("Traffic Generator INFO \r\n");
					Traffic_Gen_status();

					}
		else if (myComando.select==3)
					{
					xil_printf ("XADC INFO \r\n");
					XADC_status();

					}


		else if (myComando.select==-1)
		{
			xil_printf ("Comando inesistente \r\n");
			printMenu();
		}

		for(int i=0;i<TEST_BUFFER_SIZE;i++){
				ReceiveBuffer[i]=0;
		        }

	}



}


void Traffic_Gen_status()
{

	u32 address;
	xil_printf ("############################################################## \r\n");
	xil_printf ("#### TRAFFIC GENERATOR  \r\n");
	xil_printf ("############################################################## \r\n");
	address= TRAFGEN_BASE;
	xil_printf("|TRAFFIC GEN BASE ADDRESS\t 0x%x --- >> 0x%x\n\r", address,Xil_In32((u32)address	));
	address= TRAFGEN_BASE + STREAMING_CTRL;
	xil_printf("|STREAMING_CTRL\t\t\t 0x%x --- >> 0x%x\n\r", address,Xil_In32((u32)address	));
	address= TRAFGEN_BASE + STREAMING_CFG;
	xil_printf("|STREAMING_CFG\t\t\t 0x%x --- >> 0x%x\n\r", address,Xil_In32((u32)address	));
	xil_printf ("-------------------------------------------------------------- \r\n");
	address= TRAFGEN_BASE + STREAMING_TRANSFER_LENGTH;
	xil_printf("|STREAMING_TRANSFER_LENGTH\t 0x%x --- >> 0x%x\n\r", address,Xil_In32((u32)address	));
	address= TRAFGEN_BASE + STREAMING_TRANSFER_COUNT;
	xil_printf("|STREAMING_TRANSFER_COUNT\t 0x%x --- >> 0x%x\n\r", address,Xil_In32((u32)address	));
	address= TRAFGEN_BASE + STREAMING_ERROR_STATUS;
	xil_printf("|STREAMING_ERROR_STATUS\t\t 0x%x --- >> 0x%x\n\r", address,Xil_In32((u32)address	));
	address= TRAFGEN_BASE + STREAMING_ERROR_ENABLE;
	xil_printf("|STREAMING_ERROR_ENABLE\t\t 0x%x --- >> 0x%x\n\r", address,Xil_In32((u32)address	));
	xil_printf ("-------------------------------------------------------------- \r\n");
	address= TRAFGEN_BASE + STATIC_MODE;
	xil_printf("|STATIC_MODE\t\t\t 0x%x --- >> 0x%x\n\r", address,Xil_In32((u32)address	));
	xil_printf ("############################################################## \r\n");


}

void XADC_status()
{

		u16 vaux[16];
	//	u16 data_buffer[BUFFER_SIZE];
	//	int buffer_index = 0;
		//init_platform();

		u32 tempADC;

		const float internalScale = 3.;
		const float analogScale = 3.3;  //la scala dei canali analogici sarebbe da 0 a 1 V MA nel CMOD A35T c'è un circuito che allarga il range tra 0 e 3.3V

		Xil_Out32(XPAR_XADC_WIZ_0_BASEADDR + 0x300 , 0x9103); //40
		Xil_Out32(XPAR_XADC_WIZ_0_BASEADDR + 0x304 , 0x3F0F); //41
		Xil_Out32(XPAR_XADC_WIZ_0_BASEADDR + 0x308 , 0x0400); //42
		Xil_Out32(XPAR_XADC_WIZ_0_BASEADDR + 0x320 , 0x0800); //40

		//////////////////////////////////////////////////////////////////
		//TEMPERATURE
		tempADC = Xil_In32(XPAR_XADC_WIZ_0_BASEADDR + XADC_TEMP);
		xil_printf("TEMP = 0x%x \n\r", tempADC);
		float realT= (( ((tempADC>>4)&0x00000FFF) *503.975/4096.)-273.15);

		int whole, thousandths;
		whole = realT;
		thousandths = (realT - whole) * 1000;
		xil_printf("Temperature ----> %d.%3d \r\n", whole, thousandths);
		//////////////////////////////////////////////////////////////////

		//////////////////////////////////////////////////////////////////
		//VCCINT
		u32 measureADC = Xil_In32(XPAR_XADC_WIZ_0_BASEADDR + XADC_VCCINT);
		xil_printf("VCCINT_ADC = 0x%x \n\r", measureADC);
		realT= (( ((measureADC>>4)&0x00000FFF) *internalScale/4096.));
		whole = realT;
		thousandths = (realT - whole) * 1000;
		xil_printf("VCCINT ----> %d.%3d \r\n", whole, thousandths);
		//////////////////////////////////////////////////////////////////

		//////////////////////////////////////////////////////////////////
		//VCCAUX
		measureADC = Xil_In32(XPAR_XADC_WIZ_0_BASEADDR + XADC_VCCAUX);
		xil_printf("VCCAUX_ADC = 0x%x \n\r", measureADC);
		realT= (( ((measureADC>>4)&0x00000FFF) *internalScale/4096.));
		whole = realT;
		thousandths = (realT - whole) * 1000;
		xil_printf("VCCAUX ----> %d.%3d \r\n", whole, thousandths);
		//////////////////////////////////////////////////////////////////

		//////////////////////////////////////////////////////////////////
		//VP_VN
		measureADC = Xil_In32(XPAR_XADC_WIZ_0_BASEADDR + XADC_VPVN);
		xil_printf("VPVN_ADC = 0x%x \n\r", measureADC);
		realT= (( ((measureADC>>4)&0x00000FFF) *internalScale/4096.));
		whole = realT;
		thousandths = (realT - whole) * 1000;
		xil_printf("VPVN ----> %d.%3d \r\n", whole, thousandths);
		//////////////////////////////////////////////////////////////////

		//////////////////////////////////////////////////////////////////
		//VCCBRAM
		measureADC = Xil_In32(XPAR_XADC_WIZ_0_BASEADDR + XADC_VCCBRAM);
		xil_printf("VCCBRAM_ADC = 0x%x \n\r", measureADC);
		realT= (( ((measureADC>>4)&0x00000FFF) *internalScale/4096.));
		whole = realT;
		thousandths = (realT - whole) * 1000;
		xil_printf("VCCBRAM ----> %d.%3d \r\n", whole, thousandths);
		//////////////////////////////////////////////////////////////////



		for (int counter = 0; counter < 10; counter++)
				{
			tempADC=XSysMon_GetAdcData(InstancePtr, XSM_CH_TEMP);

			Xil_Out32(XPAR_XADC_WIZ_0_BASEADDR + 0x300 , 0x9103); //40
			Xil_Out32(XPAR_XADC_WIZ_0_BASEADDR + 0x304 , 0x3F0F); //41
			Xil_Out32(XPAR_XADC_WIZ_0_BASEADDR + 0x308 , 0x0400); //42
			Xil_Out32(XPAR_XADC_WIZ_0_BASEADDR + 0x320 , 0x0800); //40


			//////////////////////////////////////////////////////////////////
			//TEMPERATURE
			//tempADC = Xil_In32(XPAR_XADC_WIZ_0_BASEADDR + XADC_TEMP);

			xil_printf("TEMP = 0x%x \n\r", tempADC);
			realT= (( ((tempADC>>4)&0x00000FFF) *503.975/4096.)-273.15);

			int whole, thousandths;
			whole = realT;
			thousandths = (realT - whole) * 1000;
			xil_printf("Temperature ----> %d.%3d \r\n", whole, thousandths);
			//////////////////////////////////////////////////////////////////


			vaux[4] = Xil_In32(XPAR_XADC_WIZ_0_BASEADDR + 0x254);
			vaux[12] = Xil_In32(XPAR_XADC_WIZ_0_BASEADDR + 0x270);
			//xil_printf("data4 = %03dmv\n\r", vaux[4]);
			//xil_printf("data12 = %03dmv\n\r", vaux[12]);
			realT= (( ((vaux[4]>>4)&0x00000FFF) *analogScale/4096.));
			whole = realT;
			thousandths = (realT - whole) * 1000;
			xil_printf("vaux[4] ----> %d.%3d \r\n", whole, thousandths);
			realT= (( ((vaux[12]>>4)&0x00000FFF) *analogScale/4096.));
			whole = realT;
			thousandths = (realT - whole) * 1000;
			xil_printf("vaux[12] ----> %d.%3d \r\n", whole, thousandths);
			usleep(100000);
		}


	u32 address;
	xil_printf ("############################################################## \r\n");
	xil_printf ("#### XADC  \r\n");
	xil_printf ("############################################################## \r\n");
	address= XPAR_XADC_WIZ_0_BASEADDR;
	xil_printf("|XADC BASE ADDRESS\t\t 0x%x --- >> 0x%x\n\r", address,Xil_In32((u32)address	));
	address= XPAR_XADC_WIZ_0_BASEADDR + XADC_CFG1;
	xil_printf("|XADC_CFG1\t\t\t 0x%x --- >> 0x%x\n\r", address,Xil_In32((u32)address	));
	address= XPAR_XADC_WIZ_0_BASEADDR + XADC_CFG2;
	xil_printf("|XADC_CFG2\t\t\t 0x%x --- >> 0x%x\n\r", address,Xil_In32((u32)address	));

	address= XPAR_XADC_WIZ_0_BASEADDR + XADC_CFG3;
	xil_printf("|XADC_CFG3\t\t\t 0x%x --- >> 0x%x\n\r", address,Xil_In32((u32)address	));
	xil_printf ("-------------------------------------------------------------- \r\n");

	address= XPAR_XADC_WIZ_0_BASEADDR + XADC_TEMP;
	xil_printf("|XADC_TEMP\t\t\t 0x%x --- >> 0x%x\n\r", address,Xil_In32((u32)address	));
	address= XPAR_XADC_WIZ_0_BASEADDR + XADC_VCCINT;
	xil_printf("|XADC_VCCINT\t\t\t 0x%x --- >> 0x%x\n\r", address,Xil_In32((u32)address	));
	address= XPAR_XADC_WIZ_0_BASEADDR + XADC_VCCAUX;
	xil_printf("|XADC_VCCAUX\t\t\t 0x%x --- >> 0x%x\n\r", address,Xil_In32((u32)address	));
	address= XPAR_XADC_WIZ_0_BASEADDR + XADC_VPVN;
	xil_printf("|XADC_VPVN\t\t\t 0x%x --- >> 0x%x\n\r", address,Xil_In32((u32)address	));
	address= XPAR_XADC_WIZ_0_BASEADDR + XADC_VCCBRAM;
	xil_printf("|XADC_VCCBRAM\t\t\t 0x%x --- >> 0x%x\n\r", address,Xil_In32((u32)address	));


//	address= TRAFGEN_BASE + STREAMING_TRANSFER_COUNT;
//	xil_printf("|STREAMING_TRANSFER_COUNT\t 0x%x --- >> 0x%x\n\r", address,Xil_In32((u32)address	));
//	address= TRAFGEN_BASE + STREAMING_ERROR_STATUS;
//	xil_printf("|STREAMING_ERROR_STATUS\t\t 0x%x --- >> 0x%x\n\r", address,Xil_In32((u32)address	));
//	address= TRAFGEN_BASE + STREAMING_ERROR_ENABLE;
//	xil_printf("|STREAMING_ERROR_ENABLE\t\t 0x%x --- >> 0x%x\n\r", address,Xil_In32((u32)address	));
//	xil_printf ("-------------------------------------------------------------- \r\n");
//	address= TRAFGEN_BASE + STATIC_MODE;
//	xil_printf("|STATIC_MODE\t\t\t 0x%x --- >> 0x%x\n\r", address,Xil_In32((u32)address	));
	xil_printf ("############################################################## \r\n");


}



int main(void)
{



	const int NumChannels = 7;
	const Xadc_ChannelInfo CmodA7_XadcInfo[7] = {
		{0, 0, "TEMP"},
		{1, 0, "VCCINT"}, //
		{2, 0, "VCCAUX"}, //
		{3, 0, "VPVN"}, //
		{6, 0, "VCCBRAM"}, //
		{20, 0, "VAUX_4"}, //
		{28, 0, "VAUX_12"}  // VAUX_12
	};
	const int Xadc_VoltageScaler_SingleEnded = (0xFFFF) / (100 * 3.3);

	XSysMon Xadc_Ctrl;
	u16 Xadc_RawData[2];
	int Xadc_Voltage; // x100 from actual value. Two decimal point precision.
	int Xadc_VoltageRemainder;

	//EnableCaches();
	xil_printf("Initializing Cmod A7-35T XADC Demo...\n\r");
	Xadc_Init(&Xadc_Ctrl, CmodA7_XadcInfo, NumChannels, XADC_DEVICE_ID);
	xil_printf("XADC Initialized\n\r");


	int status;
	int statusCtrl;

	    // Initialize the GPIO driver
	    status = XGpio_Initialize(&Gpio, GPIO_LED_ID);
	    if (status != XST_SUCCESS) {
	        return XST_FAILURE;
	    }

	    statusCtrl = XGpio_Initialize(&GpioCtrl, GPIO_CTRL_ID);
		    if (statusCtrl != XST_SUCCESS) {
		        return XST_FAILURE;
		    }

	    // Set GPIO direction to output for the LED channel
	    XGpio_SetDataDirection(&Gpio, LED_CHANNEL, 0x0);
	    XGpio_SetDataDirection(&GpioCtrl, CTRL_CHANNEL, 0x0);

	    // Turn on the LED (set GPIO output to 1)
	    XGpio_DiscreteWrite(&Gpio, LED_CHANNEL, 0x3);


	    // Turn on counters
	    u32 ctrl_reg = (0xF |  (1  << ENABLE_COUNTERS) | (1  << INIT_COUNTERS) );
	    XGpio_DiscreteWrite(&GpioCtrl, CTRL_CHANNEL, ctrl_reg);


	/* Enable MicroBlaze exception */
    microblaze_enable_interrupts();
    xil_printf ("####################################################### \r\n");
	xil_printf ("# AXI inspector: \r\n");
	xil_printf ("# Indirizzi e dati sono a 32bit formato HEX: \r\n");
	xil_printf ("# Leggi --->  rd indirizzo \r\n");
	xil_printf ("# Scrivi--->  wr indirizzo dato\r\n");
	xil_printf ("# INFO  --->  tg TRAFFIC GENERATOR INFO\r\n");
	xil_printf ("# INFO  --->  xa XADC INFO\r\n");
	xil_printf ("####################################################### \r\n");
	/* Connect uart interrupt handler that will be called when an interrupt
	* for the uart occurs*/
	XIntc_RegisterHandler(XPAR_INTC_0_BASEADDR,XPAR_AXI_INTC_0_AXI_UARTLITE_0_INTERRUPT_INTR,(XInterruptHandler)uart_int_handler,(void *)XPAR_AXI_UARTLITE_0_BASEADDR);

	/* Start the interrupt controller */
	XIntc_MasterEnable(XPAR_INTC_0_BASEADDR);

	/* Enable uart interrupt in the interrupt controller */
	XIntc_EnableIntr(XPAR_INTC_0_BASEADDR, XPAR_AXI_UARTLITE_0_INTERRUPT_MASK);

	/* Enable Uartlite interrupt */
	XUartLite_EnableIntr(XPAR_AXI_UARTLITE_0_BASEADDR);




//	int Status = XTrafGenStremingModeMasterExample(&XTrafGenInstance,
	//		XPAR_XTRAFGEN_0_DEVICE_ID);



	/* Wait for interrupts to occur */
	while (1);
}




struct comando parser( u8* buffer)
{
	int i=0;

	struct comando t;

//	while (i < 2) //TEST_BUFFER_SIZE)
	//{
	t.commandReceived[i]=buffer[i+1];
	i++;
	t.commandReceived[i]=buffer[i+1];
	i++;
	//}
/*
	sprintf(t.cr, "%c%c",t.commandReceived[0],t.commandReceived[1]);

	switch (t.cr) {
	        case "rd":
	        	xil_printf ("Leggo \r\n");
	        	break;
	        case "wr":
	        	xil_printf ("Scrivo \r\n");
	        	break;
	        default:
	        	xil_printf ("Comando non trovato \r\n");
	            break;
	        }*/

	if (( t.commandReceived[0]=='w')&&( t.commandReceived[1]=='r'))
	{
		myComando.select=0;
	}else if (( t.commandReceived[0]=='r')&&( t.commandReceived[1]=='d'))
			{
		t.select=1;
			}
	else if (( t.commandReceived[0]=='t')&&( t.commandReceived[1]=='g'))
				{
			t.select=2;
				}
	else if (( t.commandReceived[0]=='x')&&( t.commandReceived[1]=='a'))
				{
			t.select=3;
				}
	else
		t.select=-1;



	i++; //salto lo spazio
	int j=0;

	while (i < 13)  // address
		{
		t.firstOp[j]=buffer[i+1];
			j++;
			i++;
		}

	j=0;
	if (( t.commandReceived[0]=='w')&&( t.commandReceived[1]=='r'))  // se debbo scrivere serve il dato
	{
		while (i < 26)
			{
			t.secondOp[j]=buffer[i+1];
				j++;
				i++;
			}
	}

	return t;
}





/*****************************************************************************/
/**
*
* This function demonstrates the usage Traffic Generator
* It does the following:
*       - Set up the output terminal if UART16550 is in the hardware build
*       - Initialize the AXI Traffic Generator device
*       - Initialize the Streaming FIFO device
*	- Set the Desired Transfer Count and Transfer Length
*	- Enable the Traffic Generation on the Core
*	- Check for the Streaming data on the FIFO
*       - Return test status and exit
*
* @param	InstancePtr is a pointer to the instance of the
*		XTrafGen component.
* @param	DeviceId is Device ID of the Axi Traffic Generator Device,
*
*
* @param	InstancePtr is a pointer to the instance of the
*			XTrafGen component.
* @param	DeviceId is Device ID of the Axi Traffic Generator Device,
*			typically XPAR_<TRAFGEN_instance>_DEVICE_ID value from
*			xparameters.h.
*
* @return
*			-XST_SUCCESS to indicate success
*			-XST_FAILURE to indicate failure
*
******************************************************************************/
int XTrafGenStremingModeMasterExample(XTrafGen *InstancePtr, u16 DeviceId)
{

	XTrafGen_Config *Config;
	int Status = XST_SUCCESS;
	u32 Len;
	u32 TransferCnt;
	u32 AtgPacket;
	u32 FifoOcy;
	u32 FifoLen;


	/* Initialize the Device Configuration Interface driver */
	Config = XTrafGen_LookupConfig(DeviceId);

	if (!Config) {
		xil_printf("No config found for %d\r\n", DeviceId);
		return XST_FAILURE;
	}

	/*
	 * This is where the virtual address would be used, this example
	 * uses physical address.
	 */
	Status = XTrafGen_CfgInitialize(InstancePtr, Config,
				Config->BaseAddress);
	if (Status != XST_SUCCESS) {
		xil_printf("Initialization failed\n\r");
		return Status;
	}

	/* Check for the Streaming Mode */
	if(InstancePtr->OperatingMode != XTG_MODE_STREAMING) {
		return XST_FAILURE;
	}

	/*
	 * Set the Required transaction length
	 * and required transaction count
	 */
	XTrafGen_ResetStreamingRandomLen(InstancePtr);
	XTrafGen_SetStreamingTransLen(InstancePtr , 3);
	XTrafGen_SetStreamingTransCnt(InstancePtr , 2);

	Len = XTrafGen_GetStreamingTransLen(InstancePtr);
	TransferCnt = XTrafGen_GetStreamingTransCnt(InstancePtr);

	/*
	 * Calculate the ATG data that is sent on the
	 * CORE when Streaming is Enabled
	 */
	AtgPacket = (Len +1) * TransferCnt;

	/* Enable the traffic generation */
	   xil_printf ("###############################ENABLING### \r\n");
	XTrafGen_StreamEnable(InstancePtr);
	   xil_printf ("###############################WAIT### \r\n");

	XTrafGen_StreamDisable(InstancePtr);
	   xil_printf ("###############################DISABLED### \r\n");
	return XST_SUCCESS;
}


// Initialize the XADC Wizard IP
void Xadc_Init(XSysMon *InstancePtr, const Xadc_ChannelInfo DeviceInfo[], const int NumChannels, const u32 DeviceId) {
	u32 ChannelsMask;
	u32 InputModeMask;
	int i;
	XSysMon_Config *xadc_cfg_ptr;
	xadc_cfg_ptr = XSysMon_LookupConfig(DeviceId);
	XSysMon_CfgInitialize(InstancePtr, xadc_cfg_ptr, xadc_cfg_ptr->BaseAddress);

	ChannelsMask = 0;
	InputModeMask = 0;
	for (i = 0; i < NumChannels; i++) {
		ChannelsMask |= 1 << DeviceInfo[i].SequencerIndex;
		if (DeviceInfo[i].IsDiff)
			InputModeMask |= 1 << DeviceInfo[i].SequencerIndex;
	}
	// Disable the Channel Sequencer before configuring the Sequence registers.
	XSysMon_SetSequencerMode(InstancePtr, XSM_SEQ_MODE_SINGCHAN);
	XSysMon_SetAlarmEnables(InstancePtr,0x0);
//	XSysMon_SetSeqInputMode(InstancePtr, InputModeMask);

	XSysMon_SetSequencerMode(InstancePtr, XSM_SEQ_MODE_SAFE);
	// Leave acquisition time default
	//XSysMon_SetSeqAcqTime(InstancePtr, ChannelsMask);
	// Enable all channels
	XSysMon_SetSeqChEnables(InstancePtr, XSM_SEQ_CH_TEMP| XSM_SEQ_CH_VCCINT| XSM_SEQ_CH_VCCAUX| XSM_SEQ_CH_VBRAM);

	// Leave the ADCCLK frequency default
	// Leave calibration default

	// Enable the Channel Sequencer in continuous sequencer cycling mode
	//XSysMon_SetSequencerMode(InstancePtr, XSM_SEQ_MODE_CONTINPASS);
}

// Initialize the XADC Wizard IP
//void Xadc_Init(XSysMon *InstancePtr, const Xadc_ChannelInfo DeviceInfo[], const int NumChannels, const u32 DeviceId) {
//	u32 ChannelsMask;
//	u32 InputModeMask;
//	int i;
//	XSysMon_Config *xadc_cfg_ptr;
//	xadc_cfg_ptr = XSysMon_LookupConfig(DeviceId);
//	XSysMon_CfgInitialize(InstancePtr, xadc_cfg_ptr, xadc_cfg_ptr->BaseAddress);
//
//	ChannelsMask = 0;
//	InputModeMask = 0;
//	for (i = 0; i < NumChannels; i++) {
//		ChannelsMask |= 1 << DeviceInfo[i].SequencerIndex;
//		if (DeviceInfo[i].IsDiff)
//			InputModeMask |= 1 << DeviceInfo[i].SequencerIndex;
//	}
//	// Disable the Channel Sequencer before configuring the Sequence registers.
//	XSysMon_SetSequencerMode(InstancePtr, XSM_SEQ_MODE_SAFE);
//	//XSysMon_SetSequencerMode(SysMonInstPtr, XSM_SEQ_MODE_CONTINPASS);
//	// Leave alarm enables default
//	// Leave channel averaging default
//
//	// Set single-ended input mode for all channels
//	XSysMon_SetSeqInputMode(InstancePtr, InputModeMask);
//
//	// Leave acquisition time default
//	XSysMon_SetSeqAcqTime(InstancePtr, ChannelsMask);
//	// Enable all channels
//	XSysMon_SetSeqChEnables(InstancePtr, XSM_SEQ_CH_TEMP | ChannelsMask);
//
//	// Leave the ADCCLK frequency default
//	// Leave calibration default
//
//	// Enable the Channel Sequencer in continuous sequencer cycling mode
//	XSysMon_SetSequencerMode(InstancePtr, XSM_SEQ_MODE_CONTINPASS);
//}


// Captures Data from each XADC Channel
void Xadc_ReadData(XSysMon *InstancePtr, const Xadc_ChannelInfo DeviceInfo[], const int NumChannels, u16 RawData[]) {
	//TODO: implement timeout on status check
	int i;

	// Clear the Status
	XSysMon_GetStatus(InstancePtr);

	// Wait until the End of Sequence occurs
	while ((XSysMon_GetStatus(InstancePtr) & XSM_SR_EOS_MASK) != XSM_SR_EOS_MASK);

	//Capture data from each channel, return via RawData
	for (i = 0; i < NumChannels; i++) {
		RawData[i] = XSysMon_GetAdcData(InstancePtr, DeviceInfo[i].SequencerIndex);
	}

	return;
}


